# Recorded smoke: 2026-09-03

The final run completed 10 minutes 21 seconds before Finish & Save was requested. Saving took another 67 seconds. The capture/context failure did not recur. Gameplay quality still needs work before the 30-minute acceptance rehearsal: 19 blocked actions, two rejected actions, repeated inaccessible routes, and an unfinished four-parsnip harvest objective.

## Recording and saved state

- Review copy: `broadcast/local/recordings/autoplay-smoke-2026-09-03.mp4` (11:31.11). The copy starts with the final corrected run and includes the save sequence. Debugging intervals are excluded; gameplay waits remain.
- Full raw recording: `broadcast/local/recordings/2026-09-03 19-07-39.mkv`. Earlier failed-start recording: `2026-09-03 18-54-06.mkv` in the same directory.
- Local evidence: `broadcast/local/smoke-20260903-clean/final-report.json`, `session.json`, `media-qa.log`, and screenshots. Artifacts are ignored by Git.
- Final run: `8f71aa9a-72c3-49ef-b78b-64ed75dc1548`, 13:49:23.826148–14:00:51.873083 UTC (19:19–19:30 IST). Finish & Save requested 13:59:44.485462 UTC; stop reason `finish_saved`.
- Checkpoint: `fa7003e22560428ca8c822c304bfced1`, `ControlLab_447858816`, Spring 23 Year 1, 06:00, FarmHouse, health 100, stamina 270. All five snapshot hashes match the live game-save and harness-state files.
- The restart after Rahul closed the game loaded the existing Spring 21 morning checkpoint. It did not create a new farm. Repairs then reattached to the same running game and preserved unsaved progress. The review copy starts later on Spring 21, after those repairs.
- Owned game, harness and overlay processes exited after save verification. User-owned OBS remains open; recording stopped. No public output started.

## Observed failures and fixes

1. Fullscreen DXGI capture failed with `0x887A0026`; the WinRT fallback returned an old title splash while OBS and structured state showed the live farm. This made the actor repeatedly describe a loading screen. Removed that fallback. Windowed DXGI was visually verified against live farm and menu changes. Attached runs preserve the display mode on startup, reconnect and stall recovery.
2. The normal hold menu is `GameMenu:0:InventoryPage`. Added `--attach` to use current shared objectives/notebook/world without restoring the morning checkpoint. Attach and checkpoint handoffs close that menu before planning. Attached exits attempt a normal pause and leave the game open. Foreground recovery now runs before waiting on an inactive game window.
3. A visual harvest packet exceeded the 4,500 estimated-token JSON cap after the previous trims. Added last-resort trimming of tillable candidates and older action history. Ledger, guidance, crop and inventory evidence, latest two events and last result remain covered by regression tests. The 4,500/8,000 caps remain unchanged; these are JSON estimates, not total billed prompt tokens.
4. Director medium reasoning exhausted the shared 600-token output allowance without returning a tool call. Director requests now receive 2,400 tokens; actor requests retain 600. The per-request override does not mutate the shared client.

The final recording exercised the main fixes. The subsequent code review also extended display preservation to the stall watchdog and ensured early foreground activation releases its temporary topmost state at cleanup. These two follow-ups passed offline tests; no additional paid run was made.

## Results

| Measurement | Result |
| --- | --- |
| Final run model calls | 74: 62 actor, 12 director; 74 accepted physical attempts |
| Final run errors | Zero model errors, recoverable errors or context overflows |
| Final run reported cost | $0.095079 |
| All smoke attempts reported cost | $0.149373 (sum of per-run reports, rounded to six decimals) |
| Actor latency p50 / p95 | 4.4005 / 6.967 seconds |
| Director latency p50 / p95 | 7.0175 / 18.748 seconds |
| Tool outcomes | 24 completed, 19 visual-review requests, 19 blocked, two rejected |
| Blocked/rejected share | 32.8% of tool results |
| Cached input share | 55.4% overall; actor 61.3%, director 33.2% |
| Offline regression suite | 220 tests passed; `git diff --check` clean |

The first restarted run (`ebdb29e6-57d1-4ddd-b37b-07dc42051c43`) planted four seeds: `seedsSown` 16→20. A live steer was consumed after 4.419 seconds and its pending decision discarded. Hold was consumed after 3.323 seconds; a fresh Python process attached without restarting the game. The next run (`15180203-0246-469a-9892-fdcae9b57c4d`) verified the context repair but exposed stale WinRT capture, leading to the Windowed change.

During the final run, a steer waited 60.536 seconds behind a local return/sleep controller. The autonomous sleep completed before that guidance was consumed. Local skills remain cooperative; guidance cannot interrupt each internal action. No in-process resume command was added: recovery uses Hold → fresh Python → attach. The final run required no manual gameplay rescue.

## Picture and sound

The MP4 has H.264 High video, 1920×1080 at 30 FPS, plus stereo AAC at 48 kHz. The Windowed game client is 1280×720, scaled into the existing 1536×864 game area. Objective band, audience narration/results and statistics footer are visible in beginning/middle/end frame checks. The ending shows the game's save confirmation. Audio decoded successfully; mean −34.6 dB, peak −0.1 dB. Listening review remains Rahul's acceptance check.

OBS recorded six skipped output frames and 94 skipped render frames across the raw recording/debugging session. Neither count increased between the 13:54:53 UTC baseline and recording stop. That establishes a stable final tail; it is not a zero-drop claim for the entire raw recording.

## Remaining acceptance work

Review the VOD, then address repeated blocked-route choices and the unfinished harvest using their recorded evidence. Validate faster operator response inside long local skills if required for streaming. Run the full 30-minute recording after those findings are resolved. YouTube setup, upload capacity and actual platform ingest remain separate open checks. Recovery procedure: [operator controls](operator-controls.md#repair-python-during-a-recording-or-stream).
